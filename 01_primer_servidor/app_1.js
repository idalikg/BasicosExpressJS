// Imporción del módulo express
const express = require('express');

// Inicializando servidor
const app = express();

// Corriendo servidor en el puerto 8000
app.listen(8000);

console.log('Primer ejemplo de servidor en Express');